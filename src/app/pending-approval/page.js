export default function PendingApprovalPage() {
  return (
    <main className="stack">
      <h1>Approval required</h1>
      <p className="muted">
        Your account exists, but a CRM administrator must approve your profile
        before you can read or write CRM data.
      </p>
    </main>
  );
}
